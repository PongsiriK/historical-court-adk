# ⚖️ The Historical Court (Multi-Agent System)

โปรเจกต์นี้เป็นการพัฒนาระบบ **Multi-Agent** ด้วย **Google Agent Development Kit (ADK)** เพื่อจำลองกระบวนการ "ศาลประวัติศาสตร์" โดยระบบจะทำการสืบค้นข้อมูลของบุคคลหรือเหตุการณ์สำคัญในแง่มุมที่ขัดแย้งกัน (Pros & Cons) เพื่อนำมาถกเถียงและสรุปผลอย่างเป็นกลางที่สุด

## 🏗️ System Architecture (โครงสร้างระบบ)

ระบบถูกออกแบบโดยใช้เทคนิคการผสมผสาน Agent หลายรูปแบบ (Sequential, Parallel, Loop) ตามขั้นตอนดังนี้:

### Step 1: The Inquiry (Sequential Agent)
* **หน้าที่:** เป็นด่านหน้าในการรับ input จากผู้ใช้งาน
* **การทำงาน:** Agent `historical_court_clerk` จะทักทายและสอบถามชื่อบุคคลสำคัญหรือเหตุการณ์ที่ต้องการพิจารณาคดี (เช่น *Genghis Khan*, *Cold War*) จากนั้นบันทึกหัวข้อลงใน State (`PROMPT`) เพื่อส่งต่อเข้าสู่ระบบศาล

### Step 2: The Investigation (Parallel Agent)
* **หน้าที่:** สืบพยานและหาหลักฐาน โดยแยกการทำงานเป็น 2 ฝั่งขนานกันเพื่อให้ได้ข้อมูลที่รวดเร็วและไม่ชี้นำซึ่งกันและกัน
* **Agent A (The Admirer):** มีหน้าที่ค้นหาและรวบรวมเฉพาะ "ความสำเร็จ", "ด้านบวก" หรือ "คุณงามความดี" แล้วบันทึกลงใน `pos_data`
* **Agent B (The Critic):** มีหน้าที่ค้นหาและรวบรวมเฉพาะ "ข้อผิดพลาด", "ด้านลบ", "อาชญากรรม" หรือ "ข้อโต้แย้ง" แล้วบันทึกลงใน `neg_data`

### Step 3: The Trial & Review (Loop Agent)
* **หน้าที่:** ตรวจสอบความสมดุลของหลักฐาน (Quality Control)
* **Agent C (The Judge):** ทำหน้าที่เป็นผู้พิพากษา ตรวจสอบข้อมูลใน `pos_data` และ `neg_data`
    * **เงื่อนไขการวนลูป (Feedback Loop):** หากข้อมูลฝั่งใดฝั่งหนึ่ง "น้อยเกินไป" หรือ "ไม่สมดุล" ผู้พิพากษาจะสั่งให้ Researcher (Admirer/Critic) กลับไปค้นหาใหม่ โดยระบุ `judge_feedback` ด้วย Keyword ที่เจาะจงขึ้น
    * **เงื่อนไขการจบงาน (Exit Strategy):** เมื่อข้อมูลสมบูรณ์และสมดุลทั้งสองฝั่งแล้ว ผู้พิพากษาจะเรียกใช้ Tool `exit_loop` เพื่อยุติการสืบพยาน

### Step 4: The Verdict (Output)
* **หน้าที่:** สรุปผลการพิจารณาคดี
* **การทำงาน:** Agent `verdict_writer` จะรวบรวมข้อมูลทั้งหมด นำมาเขียนรายงานสรุปเปรียบเทียบข้อเท็จจริง และใช้ Tool `write_file` บันทึกเป็นไฟล์ `.txt` ในโฟลเดอร์ `court_records/`

---

## 🛠️ Technical Implementation (รายละเอียดทางเทคนิค)

### 1. Agents Structure
| Agent Name | Type | Role | Key Tool |
| :--- | :--- | :--- | :--- |
| `historical_court_clerk` | Root | รับ Input / เริ่มต้นระบบ | `append_to_state` |
| `investigation_team` | **ParallelAgent** | คุม Agent ย่อยให้ทำงานพร้อมกัน | - |
| `admirer_agent` | Agent | หาข้อมูลด้านบวก | `WikipediaQueryRun`, `append_to_state` |
| `critic_agent` | Agent | หาข้อมูลด้านลบ | `WikipediaQueryRun`, `append_to_state` |
| `judge_agent` | Agent | ตัดสินใจ Loop / จบงาน | `exit_loop`, `append_to_state` |
| `trial_process` | **LoopAgent** | วนลูปการหาข้อมูลและการตรวจสอบ | - |
| `verdict_writer` | Agent | เขียนไฟล์สรุป | `write_file` |

### 2. State Management
ระบบใช้ **Session State** ในการส่งต่อข้อมูลระหว่าง Agent โดยแบ่ง Key ชัดเจน:
* `PROMPT`: หัวข้อที่ผู้ใช้ต้องการค้นหา
* `pos_data`: List เก็บข้อมูลด้านบวก (จาก Admirer)
* `neg_data`: List เก็บข้อมูลด้านลบ (จาก Critic)
* `judge_feedback`: คำสั่งจากผู้พิพากษาเพื่อปรับปรุงการค้นหาในรอบถัดไป

### 3. Loop Logic Constraint
* ระบบจะไม่ใช้ Prompt เพียงอย่างเดียวในการจบงาน แต่บังคับใช้ **Tool `exit_loop`** เท่านั้น
* มีการกำหนด `max_iterations=4` เพื่อป้องกัน Infinite Loop


